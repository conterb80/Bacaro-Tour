// INCOLLA QUI LA CONFIGURAZIONE WEB DI FIREBASE PER ATTIVARE LA SINCRONIZZAZIONE.
// Lascia firebaseConfig = null per usare l'app in modalità demo locale.
export const firebaseConfig = null;

// Tutti i telefoni che usano la stessa app + lo stesso Firebase vedranno gli stessi dati.
export const sharedPath = "bacaro-tour-venezia-2026";
